-- ============================================================
--  تشخيص حاسم لخطأ 500 عند التسجيل
--  نحذف محفّز التسجيل تماماً:
--   • إن نجح التسجيل بعده  → السبب كان المحفّز (وقد حللناه).
--   • إن استمرّ 500        → السبب إعدادات الإيميل في Supabase (لا قاعدة البيانات).
--  ملاحظة: المستخدم العادي لا يحتاج صفّ profiles ليتحادث — الدردشة تعمل بدونه.
--  شغّله في: Supabase → SQL Editor → Run
-- ============================================================

drop trigger if exists on_auth_user_created on auth.users;
